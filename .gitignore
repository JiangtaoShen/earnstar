# Project repos are independent git repositories
/projects/

# Raw transcripts: private evidence (hashes are public in history/ledger.jsonl)
/archive/

# Local-only
.env
.env.*
node_modules/
.claude/settings.local.json
*.tmp
