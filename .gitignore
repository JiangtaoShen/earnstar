# Project repos are independent git repositories
/projects/

# Local experiments (summarize results in history/research or session logs)
/lab/

# Owner drop folder for screenshots and files (ingested by tools/evidence.mjs, then cleared)
/inbox/

# Private evidence: raw transcripts and owner screenshots (hashes are public in history/ledger.jsonl and history/channels.json)
/archive/

# Local-only
.env
.env.*
node_modules/
.claude/settings.local.json
*.tmp
