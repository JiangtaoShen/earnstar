# Project repos are independent git repositories
/projects/

# Local experiments (summarize results in history/research or session logs)
/lab/

# Private evidence: raw transcripts (hashes are public in history/ledger.jsonl)
/archive/

# Local-only
.env
.env.*
node_modules/
.claude/settings.local.json
*.tmp
